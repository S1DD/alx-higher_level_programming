#!/usr/bin/python3

import json


"""Defines a function that writes a python object to a text file"""
def save_to_json_file(my_obj, filename):
    """
    Writes an Object to a text file, using a JSON represention"""
    with open(filename, "w") as f:
        json.dump(my_obj, f)
